import { ElementsByStringPipe } from './location-by-title-pipe';

describe('ElementsByStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ElementsByStringPipe();
    expect(pipe).toBeTruthy();
  });
});
