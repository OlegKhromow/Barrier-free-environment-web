import { LocationByStatusPipe } from './location-by-status-pipe';

describe('LocationByStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationByStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
