import { TruncateDescriptionPipePipe } from './truncate-description-pipe';

describe('TruncateDescriptionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateDescriptionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
